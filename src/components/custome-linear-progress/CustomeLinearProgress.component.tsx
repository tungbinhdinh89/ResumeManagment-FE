import { Box, LinearProgress } from "@mui/material";

function CustomeLinearProgress() {
  return (
    <Box sx={{ width: "100%" }}>
      <LinearProgress />
    </Box>
  );
}

export default CustomeLinearProgress;
